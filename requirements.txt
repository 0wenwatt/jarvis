# Core pydantic-deep framework
pydantic-deep[sandbox,web,yaml,mcp,logfire]>=0.3.35
# [web] extra pulls in starlette + uvicorn for agent.to_web() (port 7932 Web UI demo)
pydantic-ai[web]>=2.0.0

# LLM Provider Integrations
# Supports: Anthropic, OpenAI, Google Gemini, Groq, Mistral
anthropic>=0.30.0
openai>=1.50.0
google-generativeai>=0.8.0
groq>=0.10.0
mistralai>=0.4.0

# Database drivers
psycopg[binary]>=3.0
# Use this to install directly from the official Apache AGE GitHub repo
git+https://github.com/apache/age.git#subdirectory=drivers/python
asyncpg>=0.30.0
pgvector>=0.3.0

# FastAPI + Web server
fastapi>=0.125.0
uvicorn[standard]>=0.38.0
python-multipart>=0.0.21
python-dotenv>=1.0.0

# Async + concurrency
aiofiles>=23.0.0
asyncio-contextmanager>=1.0.0

# RAG stack
# LlamaIndex: used minimally — only PGVectorStore + VectorStoreIndex for vectorRAG
llama-index-core>=0.12.0
llama-index-vector-stores-postgres>=0.3.0
llama-index-embeddings-openai>=0.3.0
# Web crawling — built-in MCP server (python -m crawl4ai.mcp stdio) requires 0.9+
crawl4ai>=0.9.0
html2text>=2024.2.26

# Data processing
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.0.0
scikit-learn>=1.3.0

# CLI & TUI
click>=8.1.0
rich>=13.0.0
typer>=0.12.0
textual>=3.0.0

# Utilities
requests>=2.31.0
httpx>=0.25.0
chardet>=5.0.0
pyyaml>=6.0

# Development & debugging
ipython>=8.10.0
ipykernel>=6.20.0
jupyter>=1.0.0
pytest>=7.0.0
pytest-asyncio>=0.23.0

# Observability
logfire>=4.0

# Security & scanning
bandit>=1.8.0

# Type checking
mypy>=1.0.0
types-PyYAML>=6.0
